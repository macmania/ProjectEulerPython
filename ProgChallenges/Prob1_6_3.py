#The trip UVA/PC problem

f = input("test1.1_6")
lines = iter(f.readLine())
#start = next(lines)
x = next(lines)
for x != '0': 
	if isinstance(x, int): 
		y = [for z in range(x)]
		i = 0
		exp = next(lines)
		sumExp = 0
		while isinstance(exp, float): 
			y[i] = float()
			exp = next(lines)
			sumExp += y[i]
			i += 1
		expDiv = sumExp/len(y)
		posDif, negDif = 0, 0 

		calculateMin()
	
